import Time "mo:core/Time";

module {
  public type Timestamp = Time.Time;
  public type UserId = Nat;
  public type TransactionId = Nat;
  public type ProjectId = Nat;
  public type SupplierId = Nat;
  public type DocumentId = Nat;
  public type NotificationId = Nat;
  public type AuditLogId = Nat;
  public type AllocationId = Nat;
  public type CoaReportId = Nat;
  public type PurchaseId = Nat;
};
